let friends = ['shamim', 'seam', 'shakil'];
let number = [10, 20, 30, 40];
number.unshift(5)
// friends.push('sumaiya');
friends.pop();
console.log(number);
for(let i = 0; i< number.length; i++){
    console.log(number)
}